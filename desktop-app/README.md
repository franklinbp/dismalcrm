# DismalCRM Desktop

Cliente Electron de DismalCRM. Al abrirlo por primera vez configure
`https://dismal.vip` como URL del servidor.

```bash
npm install
npm run dist:win
```

El instalador resultante usa el identificador `com.dismal.crm.desktop`.
